{
  "name": "cc98",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp check && pnpm run check:unused && vp run -r test && vp run -r build",
    "check:unused": "node scripts/check-unused.mjs",
    "dev": "vp run website#dev",
    "prepare": "vp config"
  },
  "devDependencies": {
    "knip": "catalog:",
    "vite-plus": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.10.0",
      "onFail": "download"
    }
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
