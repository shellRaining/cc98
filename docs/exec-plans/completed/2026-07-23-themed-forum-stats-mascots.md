# 论坛统计雪球君皮肤化计划

状态：已完成

## 背景

首页右侧的论坛统计入口目前共用 `apps/website/src/assets/home/forum-stats-mascot.webp`。这张 260×260 的 WebP 来自原插图裁切，同时用于 64px 悬浮入口和 40px 面板标识。

上一轮尝试把雪球君改画成 SVG，但角色轮廓和手绘质感损失明显，方案已放弃。现有 WebP 继续作为角色标准和默认资源。后续皮肤版本只调整背景、配色和季节装饰，不重新设计雪球君。

## 目标

- 以现有 WebP 确认雪球君的基础身份，同时使用官方皮肤壁纸中的雪球君作为姿势、道具和画面风格参考。
- 为不同皮肤准备容易辨认的栅格版本，同时保证 64px 和 40px 两种显示尺寸都清楚。
- 完成 19 个实际皮肤的版本，并在人工确认后接入主题资源目录。
- 由主题资源层选择图片。`HomeForumStats.vue` 只消费当前资源，不包含具体皮肤名的判断。
- `default` 与 `summer` 共用夏季版本。专属资源缺失或加载失败时回退到现有 WebP。

## 非目标

- 不再制作或接入 SVG 版本。
- 不改变论坛统计入口的尺寸、交互、状态点和面板布局。
- 不把雪球君改成粗描边、大眼睛、圆球身体的通用兔子形象。
- 不在人工确认前把候选图接入业务代码。
- 亮暗模式默认共用同一张皮肤图，只有实际走查发现对比度问题时才增加暗色专用版本。

## 视觉约束

现有 WebP 是身份参考，官方皮肤壁纸可以作为对应皮肤的姿势、道具、场景和画面风格参考。生成时同时输入这些参考图，不使用仅靠文字描述的自由生成。

角色本体保持以下特征：

- 两只耳朵细长、接近直立，耳尖和耳根位置不变。
- 眼睛默认保持简短竖线。官方壁纸中已有明确表情时可以沿用，但不增加瞳孔、高光或睫毛。
- 允许跟随官方壁纸调整姿势和头身比例，脸型、长耳、嘴部和手绘轮廓仍需让角色一眼可认。
- 轮廓保持偏细的手绘线条，保留耳内和脸颊的排线质感。
- 角色在画面中的位置和占比接近原图，不能被道具遮住眼睛、耳朵或主要轮廓。

皮肤版本允许调整姿势、背景、主色、天气、植物和手持道具。装饰应保持少量、轮廓清楚，不放文字，不堆叠缩小后无法辨认的细节。游泳圈只属于夏季版本，其他皮肤按对应壁纸选择场景和道具。夏季优先保留现有海面和游泳圈构图，只做必要的清晰度修复。

## 资源与接入方案

候选图和生成记录都保留在视觉走查 worktree 内：

- 生成候选：`.artifacts/imagegen/2026-07-23-themed-forum-stats-mascots/`
- 浏览器截图：`.artifacts/browser/2026-07-23-themed-forum-stats-mascots/`
- 审核通过的资源：`apps/website/src/assets/themes/{skin}/forum-stats-mascot.webp`

候选图以不低于 1024×1024 的正方形栅格图生成或编辑，审核后导出 WebP。提交到仓库的 19 张成品统一为 520×520。生成源图和淘汰稿保留在 `.artifacts/`，不提交到仓库。用户确认全部候选后，Draft PR 中的临时审核图和压缩总览已删除，确认版本移入主题资源目录。

接入时新增薄的主题资源注册层，从资源路径建立 `SkinId` 到图片 URL 的映射。组件只读取解析后的 URL。解析规则如下：

1. `default` 解析为 `summer`。
2. 当前皮肤存在专属图片时使用专属图片。
3. 没有专属图片时使用 `apps/website/src/assets/home/forum-stats-mascot.webp`。

如果实现阶段发现现有皮肤元信息表能自然承载资源 URL，可以把映射合并到该表；否则保留独立注册模块，不为单个图标建立通用主题资产框架。

## 实施步骤

### 第一阶段：建立参考和审核模板

- [x] 整理现有 WebP、官方壁纸和角色裁图，作为轮廓、五官、排线和构图参考。
- [x] 生成 64px 悬浮入口和 40px 面板标识的裁切预览，作为清晰度基线。
- [x] 准备候选对比拼图模板，每张图同时展示方形源图、64px 和 40px 圆形裁切效果。

### 第二阶段：生成四季代表版本

- [x] 春、夏、秋、冬各生成一小批候选，同一批只改变一个主要装饰方向。
- [x] 对每张候选检查角色不变量，淘汰脸型、耳朵、眼睛、线条或头身比例漂移的结果。
- [x] 把合格候选整理成对比拼图，请用户逐个确认。未确认的图片不进入源码目录。
- [x] 将确认版本导出为 WebP，并记录最终尺寸、编码和文件体积。

### 第三阶段：接入主题资源层

- [x] 建立皮肤资源映射和原图回退逻辑，`default` 映射到 `summer`。
- [x] 调整论坛统计组件，让 64px 入口和 40px 面板标识共用解析后的当前资源。
- [x] 为映射、默认别名和缺失资源回退补充有实际分支价值的测试。
- [x] 确认切换皮肤后图片立即更新，不需要刷新页面。

### 第四阶段：扩展其余皮肤

- [x] 按主题相近程度分批处理其余皮肤，优先制作中秋、春节、端午、清明、重阳等辨识度高的节日版本。
- [x] 再处理春樱日和、冬日暖雪、秋色之空、瀚海等风格型皮肤，避免只换背景颜色却缺少主题特征。
- [x] 每批继续执行候选拼图和人工确认门槛，不沿用未确认的生成方向。
- [x] 用户确认全部 19 套候选；原图只保留为未知资源或加载失败时的回退。

## 验证

图片验收需要同时满足：角色特征与原图一致；64px 下能辨认雪球君和主装饰；40px 下五官和耳朵不糊；圆形裁切不截断耳朵；背景与状态点、边框和皮肤主色没有明显冲突。

浏览器走查覆盖以下场景：

- `default`、春、夏、秋、冬和春节，并逐项检查全部 19 个实际皮肤。
- 亮色和暗色模式，包含一组亮暗配对皮肤。
- 悬浮入口的默认、悬停和聚焦状态，以及面板展开后的 40px 标识。
- 1440px 和 1140px 视口，检查破图、布局偏移和控制台错误。
- 触发图片加载错误事件，确认组件会稳定回退到原图。

实施过程中每个切片完成后运行 `vp check`。全部图片和接入逻辑验收后运行 `vp run ready`。浏览器证据写入 `.artifacts/browser/2026-07-23-themed-forum-stats-mascots/`，不提交仓库。

## 进展与调整

- 2026-07-23：确认放弃 SVG 方案，现有 WebP 保持不变。
- 2026-07-23：本计划写入 `fix/frontend-visual-review` 对应 worktree，尚未开始生成候选图或修改业务代码。
- 2026-07-23：第一组四季稿错误地保留了全部游泳圈，只作为淘汰稿保存在 `.artifacts/imagegen/`。
- 2026-07-23：生成口径调整为原图确认身份、官方壁纸提供姿势和风格。第二组已生成春季野餐、秋季烤胡萝卜、小雪夜景和春节四张候选，等待人工选择。
- 2026-07-23：源图格式统一为方形满版画布，圆形外观交给界面裁切。春季和秋季已按新口径重做。
- 2026-07-23：19 个实际皮肤已补齐候选。16 张使用原图和对应官方壁纸重新生成，轻雪和春节沿用第二组，夏季保留现有游泳圈原图。完整总览和 64px、40px 圆裁预览保存在 `.artifacts/imagegen/2026-07-23-themed-forum-stats-mascots/seasonal-round3-full-set/previews/`，当前等待人工挑选，尚未接入业务代码。
- 2026-07-23：为 GitHub Draft PR 导出 19 张 520×520 审核用 WebP 和三张压缩总览，合计约 1MB。审核资产只用于逐张评论，选定最终版本后会在合入前删除。
- 2026-07-24：用户确认全部 19 套候选。审核图已从执行计划资产目录删除，成品移入各皮肤资源目录。
- 2026-07-24：主题资源映射、`default` 别名、原图回退和组件接入已完成。单元测试覆盖全部皮肤、默认别名和未知资源回退。
- 2026-07-24：浏览器走查覆盖 1440px 与 1140px、亮暗模式、四季与春节皮肤、悬停、焦点和面板展开。全部皮肤图片请求均为 200，控制台无新增错误。

## 决策记录

- 2026-07-23：原图是角色标准和默认回退，皮肤化只改变环境与装饰。
- 2026-07-23：使用高分辨率栅格参考编辑并输出 WebP，不再追求 SVG。
- 2026-07-23：图片按批次人工确认，确认前不接入页面。
- 2026-07-23：主题资源选择集中管理，业务组件不硬编码皮肤名称。
- 2026-07-23：候选源图统一为方形，悬浮入口使用圆形裁切，面板标识可以继续使用圆角方形裁切，两处共用同一张皮肤资源。
- 2026-07-24：最终资源统一为 520×520、有损 VP8 WebP。19 张图片共 729,812 字节，单张范围为 11,164 至 70,176 字节。

## 实施结果

- 19 张雪球君成品位于 `apps/website/src/assets/themes/{skin}/forum-stats-mascot.webp`，总计 712.7 KiB。
- `apps/website/src/stores/skins.ts` 通过精确的 `import.meta.glob` 建立资源映射，`default` 复用 `summer`，未知资源回退到原图。
- `HomeForumStats.vue` 根据当前皮肤解析图片，64px 入口和 40px 面板标识共用同一 URL，加载失败时回退到原图。
- Draft PR 中用于人工审核的临时图片已删除，生成源图、淘汰稿和浏览器证据只保留在 `.artifacts/`。

## 验证结果

- `vp check --fix`：通过。
- `vp run website#test`：通过，共 30 个测试文件、285 个测试。
- `agent-browser`：全部 19 个实际皮肤均加载 520×520 成品，切换无需刷新；1440px 与 1140px、亮暗模式、悬停、焦点和面板展开均通过。
- 加载错误回退：触发入口图片的 `error` 事件后，资源切换到 260×260 原图。
- `vp run ready`：通过，包含格式、lint、类型检查、Knip、全量测试和构建。

## 遗留项

无。
